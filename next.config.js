/** @type {import('next').NextConfig} */
const nextConfig = {
	fs: "empty",
	reactStrictMode: true,
	// i18n: {
	//   locales: ['en', 'id'],
	//   defaultLocale: 'en'
	// },
};

module.exports = nextConfig;
